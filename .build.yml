##########################################################
# THE FOLLOWING LINES IS USED BY docker-build
##########################################################
requires:
  debian:
    # Useful URL: https://github.com/cafe-desktop/debian-packages
    # Useful URL: https://salsa.debian.org/debian-cafe-team/croma
    - autoconf-archive
    - autopoint
    - clang
    - clang-tools
    - cppcheck
    - expect #required for unbuffer
    - gcc
    - gir1.2-atk-1.0 #required for ctk
    - git
    - intltool
    - libatk-bridge2.0-dev #required for ctk
    - libcanberra-gtk3-dev
    - libegl1-mesa-dev #required for ctk
    - libepoxy-dev #required for ctk
    - libgdk-pixbuf2.0-dev #required for ctk
    - libglib2.0-dev
    - libgtop2-dev
    - libice-dev
    - libpango1.0-dev
    - librest-0.7-0 #required for ctk
    - libsm-dev
    - libstartup-notification0-dev
    - libtool
    - libwayland-dev #required for ctk
    - libx11-dev
    - libxcomposite-dev
    - libxcursor-dev
    - libxdamage-dev
    - libxext-dev
    - libxfixes-dev
    - libxinerama-dev
    - libxkbcommon-dev #required for ctk
    - libxpresent-dev
    - libxrandr-dev
    - libxrender-dev
    - libxt-dev
    - make
    - wayland-protocols #required for ctk
    - meson
    - wget
    - x11proto-present-dev
    - yelp-tools
    - zenity

before_scripts:
  - ./html-cppcheck
  - ./install-from-git https://github.com/cafe-desktop/cafe-common master
  - ./install-from-debs https://github.com/cafe-desktop/ctk master libctk-3-dev gir1.2-ctk-3.0 libctk-3-common libctk-3-0

build_scripts:
  - ./before-build
  - ./make-scanbuild '--enable-compile-warnings=maximum'
  - ./after-build

after_scripts:
  - if [ -f "gen-index" ]; then ./gen-index -l 20 -i https://github.com/${OWNER_NAME}/cafe-icon-theme/raw/master/cafe/16x16/apps/preferences-system-windows.png; fi
